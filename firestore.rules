rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Simple rule for initial connection and seeding
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
